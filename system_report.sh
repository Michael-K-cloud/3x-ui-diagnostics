#!/bin/bash
# Отчёт о состоянии сервера в терминале (версия 3.0).
# Тонкая обёртка: печатает ТОТ ЖЕ текст, что и HTML-отчёт (единый источник — report.sh),
# в конце создаёт HTML и печатает ссылку.
exec bash /root/scripts/report.sh status --print
